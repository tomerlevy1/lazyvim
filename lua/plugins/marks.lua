return {
  {
    "chentoast/marks.nvim",
    opts = {
      -- whether to map keybinds or not. default true
      default_mappings = false,
      mappings = {
        set_next = "mn",
        toggle = "mt",
        delete_buf = "dmm",
        next = "<C-m>"
      }
    }
  }
}
